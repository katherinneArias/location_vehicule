<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <nav style="background: #eee; padding: 10px;">
        {% if app.user %}
            Conectado como <strong>{{ app.user.email }}</strong>
            {% if 'ROLE_ADMIN' in app.user.roles %}
                (Administrador)
            {% else %}
                (Cliente)
            {% endif %}
            <a href="{{ path('app_logout') }}" style="margin-left: 10px;">Cerrar sesión</a>
        {% else %}
            <a href="{{ path('app_login') }}">Iniciar sesión</a>
            <a href="{{ path('app_register') }}" style="margin-left: 10px;">Registrarse</a>
        {% endif %}
    </nav>

    {% block body %}{% endblock %}
</body>

</html>
